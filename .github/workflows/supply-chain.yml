name: Supply chain

on:
  push:
    branches: [main]
    paths:
      - Dockerfile
      - requirements.in
      - requirements.lock.txt
      - base-images.lock.json
      - pyproject.toml
      - server.json
      - lib/**
      - .github/workflows/supply-chain.yml
  pull_request:
    branches: [main]
  workflow_dispatch:

# Least-privilege default for GITHUB_TOKEN; this workflow only reads the repo.
permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"
      - name: Verify requirements lockfile
        run: python3 lib/verify_requirements.py
      - name: Verify base images are digest-pinned
        run: python3 lib/verify_base_images.py
      - name: Verify version is single-sourced from pyproject
        run: python3 lib/verify_version.py
      # Same gate the publish workflow runs. It lived only there, so a
      # lockfile with a known advisory could merge unseen and only failed
      # after landing on main, which is exactly what happened with
      # cryptography 49.0.0 and mcp 1.28.0. Running it here fails the pull
      # request instead.
      - name: Gate on a clean dependency vulnerability scan
        uses: ./.github/actions/vuln-scan
